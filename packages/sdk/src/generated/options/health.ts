// Auto-generated by generate-options.js — DO NOT EDIT
import * as v from "valibot";
import type { Client } from "../../types";

export function getHelloOptions(config: {
  client: Client;
  credentials?: RequestCredentials;
  headers?: HeadersInit;
}) {
  return {
    queryKey: ["health", "getHello"] as const,
    queryFn: async () => {
      const response = await config.client.get("/api/v1", {
        credentials: config.credentials ?? "include",
        headers: config.headers,
      });
      return response.data as any;
    },
  };
}

export function healthCheckOptions(config: {
  client: Client;
  credentials?: RequestCredentials;
  headers?: HeadersInit;
}) {
  return {
    queryKey: ["health", "healthCheck"] as const,
    queryFn: async () => {
      const response = await config.client.get("/api/v1/health", {
        credentials: config.credentials ?? "include",
        headers: config.headers,
      });
      return response.data as any;
    },
  };
}
