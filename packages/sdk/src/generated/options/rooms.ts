// Auto-generated by generate-options.js — DO NOT EDIT
import * as v from "valibot";
import type { Client } from "../../types";
import { roomsController_createRoomResponseSchema, createRoomDtoSchema, roomResponseDtoSchema, joinRoomDtoSchema, roomsController_leaveRoomBodySchema, roomsController_chooseMovieBodySchema, roomMembersResponseDtoSchema, roomsController_getChatHistoryResponseSchema, roomsController_muteUserBodySchema, type RoomsController_createRoomResponse, type CreateRoomDto, type RoomResponseDto, type JoinRoomDto, type RoomsController_leaveRoomBody, type RoomsController_chooseMovieBody, type RoomMembersResponseDto, type RoomsController_getChatHistoryResponse, type RoomsController_muteUserBody } from "../schemas";

export function createRoomOptions(config: {
  client: Client;
  credentials?: RequestCredentials;
  headers?: HeadersInit;
}) {
  return {
    mutationKey: ["rooms", "createRoom"] as const,
    mutationFn: async (body: CreateRoomDto) => {
      const response = await config.client.post("/api/v1/rooms", body, {
        credentials: config.credentials ?? "include",
        headers: config.headers,
      });
      return v.parse(roomsController_createRoomResponseSchema, response.data);
    },
  };
}

export function getMyRoomsOptions(config: {
  client: Client;
  credentials?: RequestCredentials;
  headers?: HeadersInit;
}) {
  return {
    queryKey: ["rooms", "getMyRooms"] as const,
    queryFn: async () => {
      const response = await config.client.get("/api/v1/rooms/my-rooms", {
        credentials: config.credentials ?? "include",
        headers: config.headers,
      });
      return v.parse(v.array(roomResponseDtoSchema), response.data);
    },
  };
}

export function getRoomOptions(config: {
  client: Client;
  path: { id: string };
  credentials?: RequestCredentials;
  headers?: HeadersInit;
}) {
  return {
    queryKey: ["rooms", "getRoom", config.path] as const,
    queryFn: async () => {
      const response = await config.client.get(`/api/v1/rooms/${config.path.id}`, {
        credentials: config.credentials ?? "include",
        headers: config.headers,
      });
      return v.parse(roomResponseDtoSchema, response.data);
    },
  };
}

export function joinRoomOptions(config: {
  client: Client;
  credentials?: RequestCredentials;
  headers?: HeadersInit;
}) {
  return {
    mutationKey: ["rooms", "joinRoom"] as const,
    mutationFn: async (body: JoinRoomDto) => {
      const response = await config.client.post("/api/v1/rooms/join", body, {
        credentials: config.credentials ?? "include",
        headers: config.headers,
      });
      return v.parse(roomResponseDtoSchema, response.data);
    },
  };
}

export function leaveRoomOptions(config: {
  client: Client;
  path: { id: string };
  credentials?: RequestCredentials;
  headers?: HeadersInit;
}) {
  return {
    mutationKey: ["rooms", "leaveRoom", config.path] as const,
    mutationFn: async (body: RoomsController_leaveRoomBody) => {
      const response = await config.client.post(`/api/v1/rooms/${config.path.id}/leave`, body, {
        credentials: config.credentials ?? "include",
        headers: config.headers,
      });
      return v.parse(roomResponseDtoSchema, response.data);
    },
  };
}

export function chooseMovieOptions(config: {
  client: Client;
  path: { id: string };
  credentials?: RequestCredentials;
  headers?: HeadersInit;
}) {
  return {
    mutationKey: ["rooms", "chooseMovie", config.path] as const,
    mutationFn: async (body: RoomsController_chooseMovieBody) => {
      const response = await config.client.post(`/api/v1/rooms/${config.path.id}/choice`, body, {
        credentials: config.credentials ?? "include",
        headers: config.headers,
      });
      return v.parse(roomResponseDtoSchema, response.data);
    },
  };
}

export function getRoomMembersOptions(config: {
  client: Client;
  path: { id: string };
  credentials?: RequestCredentials;
  headers?: HeadersInit;
}) {
  return {
    queryKey: ["rooms", "getRoomMembers", config.path] as const,
    queryFn: async () => {
      const response = await config.client.get(`/api/v1/rooms/${config.path.id}/members`, {
        credentials: config.credentials ?? "include",
        headers: config.headers,
      });
      return v.parse(roomMembersResponseDtoSchema, response.data);
    },
  };
}

export function removeUserFromRoomOptions(config: {
  client: Client;
  path: { id: string; userId: string };
  credentials?: RequestCredentials;
  headers?: HeadersInit;
}) {
  return {
    mutationKey: ["rooms", "removeUserFromRoom", config.path] as const,
    mutationFn: async () => {
      const response = await config.client.delete(`/api/v1/rooms/${config.path.id}/members/${config.path.userId}`, {
        credentials: config.credentials ?? "include",
        headers: config.headers,
      });
      return v.parse(roomResponseDtoSchema, response.data);
    },
  };
}

export function getChatHistoryOptions(config: {
  client: Client;
  path: { id: string };
  credentials?: RequestCredentials;
  headers?: HeadersInit;
}) {
  return {
    queryKey: ["rooms", "getChatHistory", config.path] as const,
    queryFn: async () => {
      const response = await config.client.get(`/api/v1/rooms/${config.path.id}/chat/history`, {
        credentials: config.credentials ?? "include",
        headers: config.headers,
      });
      return v.parse(roomsController_getChatHistoryResponseSchema, response.data);
    },
  };
}

export function muteUserOptions(config: {
  client: Client;
  path: { id: string; userId: string };
  credentials?: RequestCredentials;
  headers?: HeadersInit;
}) {
  return {
    mutationKey: ["rooms", "muteUser", config.path] as const,
    mutationFn: async (body: RoomsController_muteUserBody) => {
      const response = await config.client.post(`/api/v1/rooms/${config.path.id}/members/${config.path.userId}/mute`, body, {
        credentials: config.credentials ?? "include",
        headers: config.headers,
      });
      return v.parse(roomResponseDtoSchema, response.data);
    },
  };
}

export function unmuteUserOptions(config: {
  client: Client;
  path: { id: string; userId: string };
  credentials?: RequestCredentials;
  headers?: HeadersInit;
}) {
  return {
    mutationKey: ["rooms", "unmuteUser", config.path] as const,
    mutationFn: async () => {
      const response = await config.client.delete(`/api/v1/rooms/${config.path.id}/members/${config.path.userId}/mute`, {
        credentials: config.credentials ?? "include",
        headers: config.headers,
      });
      return v.parse(roomResponseDtoSchema, response.data);
    },
  };
}
