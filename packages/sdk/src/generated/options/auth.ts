// Auto-generated by generate-options.js — DO NOT EDIT
import * as v from "valibot";
import type { Client } from "../../types";
import { loginResponseDtoSchema, loginDtoSchema, adminLoginDtoSchema, type LoginResponseDto, type LoginDto, type AdminLoginDto } from "../schemas";

export function loginOptions(config: {
  client: Client;
  credentials?: RequestCredentials;
  headers?: HeadersInit;
}) {
  return {
    mutationKey: ["auth", "login"] as const,
    mutationFn: async (body: LoginDto) => {
      const response = await config.client.post("/api/v1/auth/login", body, {
        credentials: config.credentials ?? "include",
        headers: config.headers,
      });
      return v.parse(loginResponseDtoSchema, response.data);
    },
  };
}

export function refreshTokenOptions(config: {
  client: Client;
  credentials?: RequestCredentials;
  headers?: HeadersInit;
}) {
  return {
    mutationKey: ["auth", "refreshToken"] as const,
    mutationFn: async () => {
      const response = await config.client.post("/api/v1/auth/refresh", undefined, {
        credentials: config.credentials ?? "include",
        headers: config.headers,
      });
      return response.data as any;
    },
  };
}

export function logoutOptions(config: {
  client: Client;
  credentials?: RequestCredentials;
  headers?: HeadersInit;
}) {
  return {
    mutationKey: ["auth", "logout"] as const,
    mutationFn: async () => {
      const response = await config.client.post("/api/v1/auth/logout", undefined, {
        credentials: config.credentials ?? "include",
        headers: config.headers,
      });
      return response.data as any;
    },
  };
}

export function adminLoginOptions(config: {
  client: Client;
  credentials?: RequestCredentials;
  headers?: HeadersInit;
}) {
  return {
    mutationKey: ["auth", "adminLogin"] as const,
    mutationFn: async (body: AdminLoginDto) => {
      const response = await config.client.post("/api/v1/auth/admin/login", body, {
        credentials: config.credentials ?? "include",
        headers: config.headers,
      });
      return v.parse(loginResponseDtoSchema, response.data);
    },
  };
}
