// Auto-generated by generate-options.js — DO NOT EDIT
import * as v from "valibot";
import type { Client } from "../../types";
import { redisUserResponseDtoSchema, createRedisUserDtoSchema, userProfileResponseDtoSchema, userResponseDtoSchema, usersController_updateNameBodySchema, type RedisUserResponseDto, type CreateRedisUserDto, type UserProfileResponseDto, type UserResponseDto, type UsersController_updateNameBody } from "../schemas";

export function createUserOptions(config: {
  client: Client;
  credentials?: RequestCredentials;
  headers?: HeadersInit;
}) {
  return {
    mutationKey: ["users", "createUser"] as const,
    mutationFn: async (body: CreateRedisUserDto) => {
      const response = await config.client.post("/api/v1/users", body, {
        credentials: config.credentials ?? "include",
        headers: config.headers,
      });
      return v.parse(redisUserResponseDtoSchema, response.data);
    },
  };
}

export function getProfileOptions(config: {
  client: Client;
  credentials?: RequestCredentials;
  headers?: HeadersInit;
}) {
  return {
    queryKey: ["users", "getProfile"] as const,
    queryFn: async () => {
      const response = await config.client.get("/api/v1/users/profile", {
        credentials: config.credentials ?? "include",
        headers: config.headers,
      });
      return v.parse(userProfileResponseDtoSchema, response.data);
    },
  };
}

export function getAllUsersOptions(config: {
  client: Client;
  query?: { id?: string; name?: string };
  credentials?: RequestCredentials;
  headers?: HeadersInit;
}) {
  return {
    queryKey: ["users", "getAllUsers", config.query] as const,
    queryFn: async () => {
      const response = await config.client.get("/api/v1/users/all", {
        credentials: config.credentials ?? "include",
        headers: config.headers,
        params: config.query as any,
      });
      return v.parse(v.array(userResponseDtoSchema), response.data);
    },
  };
}

export function getUserOptions(config: {
  client: Client;
  path: { userId: string };
  credentials?: RequestCredentials;
  headers?: HeadersInit;
}) {
  return {
    queryKey: ["users", "getUser", config.path] as const,
    queryFn: async () => {
      const response = await config.client.get(`/api/v1/users/${config.path.userId}`, {
        credentials: config.credentials ?? "include",
        headers: config.headers,
      });
      return v.parse(redisUserResponseDtoSchema, response.data);
    },
  };
}

export function deleteUserOptions(config: {
  client: Client;
  path: { userId: string };
  credentials?: RequestCredentials;
  headers?: HeadersInit;
}) {
  return {
    mutationKey: ["users", "deleteUser", config.path] as const,
    mutationFn: async () => {
      const response = await config.client.delete(`/api/v1/users/${config.path.userId}`, {
        credentials: config.credentials ?? "include",
        headers: config.headers,
      });
      return response.data as any;
    },
  };
}

export function updateNameOptions(config: {
  client: Client;
  path: { userId: string };
  credentials?: RequestCredentials;
  headers?: HeadersInit;
}) {
  return {
    mutationKey: ["users", "updateName", config.path] as const,
    mutationFn: async (body: UsersController_updateNameBody) => {
      const response = await config.client.patch(`/api/v1/users/${config.path.userId}/name`, body, {
        credentials: config.credentials ?? "include",
        headers: config.headers,
      });
      return v.parse(redisUserResponseDtoSchema, response.data);
    },
  };
}
