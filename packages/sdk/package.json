{
  "name": "@mm-preview/sdk",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "biome check",
    "type-check": "tsc --noEmit",
    "generate:swagger": "node scripts/download-swagger.js",
    "generate:types": "node scripts/generate-types.js",
    "generate:requests": "node scripts/generate-requests.js",
    "generate:hooks": "node scripts/generate-hooks.js",
    "generate:all": "npm run generate:swagger && npm run generate:types && npm run generate:requests && npm run generate:hooks"
  },
  "dependencies": {
    "@hey-api/client-fetch": "^0.7.0",
    "@tanstack/react-query": "^5.90.2",
    "socket.io-client": "^4.8.3"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.0",
    "@openapitools/openapi-generator-cli": "^2.7.0",
    "@types/node": "^20",
    "@types/react": "^19.2.3",
    "openapi-typescript": "^7.0.0",
    "typescript": "^5"
  },
  "peerDependencies": {
    "react": "^19.2.3"
  }
}
